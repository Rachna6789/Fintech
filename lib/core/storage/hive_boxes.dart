class HiveBoxes {
  const HiveBoxes._();

  static const portfolio = 'portfolio_cache';
  static const market = 'market_cache';
  static const settings = 'settings_cache';
  static const recentSearches = 'market_searches';
  static const offlineQueue = 'offline_queue';

  static const all = [
    portfolio,
    market,
    settings,
    recentSearches,
    offlineQueue,
  ];
}
